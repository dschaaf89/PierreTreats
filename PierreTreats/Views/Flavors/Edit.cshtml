@{
  Layout = "_Layout";
}

@model PierreTreats.Models.Flavor
@Html.ActionLink(" All Flavors", "Index", null, null, new { @class="btn btn-info fa fa-user" })
<h2>Edit</h2>

<h4>Edit @Html.DisplayFor(model => model.FlavorType)</h4>

@using (Html.BeginForm())
{
  @Html.HiddenFor(model => model.FlavorId)

  @Html.LabelFor(model => model.FlavorType)
  @Html.EditorFor(model => model.FlavorType, new { @required = "required"})
 
  <button type="submit" class="btn btn-info fa"><i class="fa-edit"> Save</i></button>
}
