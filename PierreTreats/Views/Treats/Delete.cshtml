@{
  Layout = "_Layout";
}

@model PierreTreats.Models.Treats
@Html.ActionLink(" All Treats", "Index", null, null, new { @class="btn btn-info fa fa-user" })
<h2>Are you sure you want to delete this Treat out of inventory?</h2>

@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)
@using (Html.BeginForm())
{
  <button type="submit" class="btn btn-info fa "><i class="fa-times"> Delete</i></button>
}